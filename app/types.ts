export type userType = {
    username: string,
    password: string,
    isAdmin: boolean
}
export type UserT = {
    username: string,
      email?: string,
      password: string,
      img?: string,
      isAdmin?: boolean,
      isActive?: boolean,
      phone?: string,
      address?: string,
      }//?